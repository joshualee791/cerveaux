# Copy to .env.local (or .env) — never commit real values.
# Playbook §17

DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate with: openssl rand -base64 32"

# bcrypt hash of your password (cost factor 12 per playbook) — generate e.g.:
# node -e "require('bcrypt').hash('your-password', 12).then(console.log)"
HASHED_PASSWORD=""

# Anthropic (Marie — Claude) — playbook §17
ANTHROPIC_API_KEY=""
# Optional override; default in code is claude-sonnet-4-20250514
# ANTHROPIC_MODEL=""
# Router (Haiku) — optional; default claude-haiku-4-5
# ANTHROPIC_ROUTER_MODEL=""
# Memory summarization (Haiku) — optional; defaults to ROUTER_MODEL then claude-haiku-4-5
# ANTHROPIC_MEMORY_MODEL=""

# OpenAI (Roy) — playbook §17
OPENAI_API_KEY=""
# Optional override; default in code is gpt-4.1 (instruction-following; see OpenAI model docs)
# OPENAI_MODEL=""
